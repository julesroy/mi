on:
  push:
    branches:
      - deployment
name: Publish
jobs:
  FTP-Deploy-Action:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.1.0
        with:
          fetch-depth: 2
      - name: FTP-Deploy-Action
        uses: SamKirkland/FTP-Deploy-Action@4.3.3
        with:
          server: ftp.cluster021.hosting.ovh.net
          username: nlntmyr
          password: MaisonISENIT2025
          local-dir: ./
          server-dir: www/